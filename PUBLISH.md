# 올리는 법

이 저장소는 직접 고친다. 앱 소스는 들어 있지 않다 — `src/sprites.js`는 랜딩이
햄스터를 그리는 데 쓰는 도트 데이터고, `demo/`는 브라우저에서 돌아가게 따로
손본 웹 데모다.

1. Settings → Pages → Source: main 브랜치 / 루트(`/`)
2. Releases에 `.dmg`를 올리고, `site.js`의 `REPO`와 `DOWNLOAD`를 채운다
