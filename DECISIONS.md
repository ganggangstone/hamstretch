# 결정 기록 (ADR)

이 문서는 **왜 그렇게 정했는지**만 적는다.

결정이 바뀌면 여기 항목을 고치고 바뀐 이유를 덧붙인다. **지우지는 않는다.**
"왜 그때 그걸 안 골랐는지"를 잊으면 같은 논의를 처음부터 반복하게 된다.

> 이 문서는 공개본입니다. 배포 비용·판매·사업 관련 결정은 빼고 올렸습니다.
> 번호는 원본을 따르므로 중간에 비는 자리가 있습니다.

## 이 문서를 읽는 법

**`결정` 줄은 누가 정했는지를 적는다.** 이 프로젝트는 사람과 AI가 나눠 만들었고,
나중에 결정을 뒤집을 때 무엇을 다시 따져야 하는지가 달라서 구분해 기록했다.

| 표기 | 뜻 | 뒤집을 때 |
|---|---|---|
| **개발자** | 취향·비용·제품 방향. 사람이 정한 것 | 근거가 사람의 사정이므로 함부로 뒤집지 않는다 |
| **AI** | 기술적 근거로 정한 것 | 근거가 사라지면 바로 뒤집어도 된다 |

본문의 "사용자"는 **앱을 쓰는 사람**을 뜻한다. 만든 사람은 "개발자"로 적었다.

⭐가 붙은 항목은 **한번 정했다가 근거를 보고 뒤집은** 결정이다.
이 프로젝트에서 가장 값어치 있는 기록이라고 보고 따로 표시했다.

## 숫자로 확인한 것

추측으로 정하지 않고 재본 것들이다. **두 번은 재본 결과가 원래 판단을 뒤집었다.**

| 무엇 | 결과 | 그래서 |
|---|---|---|
| 전체 화면 투명 창을 상시 띄웠을 때 유휴 CPU | **1.10%** | 원인이 애니메이션인 줄 알았는데 아니었다 |
| 대기 중 창을 240×240으로 줄인 뒤 | **0.43%** | 비용은 상시 합성되는 **창 면적**에서 나온다 (2번) |
| 마우스 위치 1초마다 확인하는 비용 | **+0.005%p** | 같은 조건 재측정 시 기준선이 ±0.017%p 흔들렸다. **잡음보다 작아 "차이 없음"** (실측 전에는 단점으로 적었다) |
| 릴리즈 바이너리 | **4.2MB** | Electron이면 150MB대 (1번) |
| 메뉴바 아이콘 4종 | **약 1KB** | 코드에 그대로 박아 넣어도 부담 없다 |

측정 과정에서 한 번 크게 틀렸다. 앱의 WebKit 렌더러까지 합산해 쟀더니
**감시를 켠 쪽이 더 낮게** 나왔다. 일을 더 시켰는데 비용이 줄 수는 없다.
프로세스를 잘못 골랐던 것이고, 이후로는 **결과가 물리적으로 불가능하면 측정 방법을 먼저 의심한다.**

## 한눈에 보기

| | 무엇을 정했나 |
|---|---|
| 1 | [무엇으로 만들 것인가 (프레임워크)](#1-무엇으로-만들-것인가-프레임워크) |
| 2 | [펫 창을 어떤 크기로 띄울 것인가](#2-펫-창을-어떤-크기로-띄울-것인가--실측으로-뒤집힌-결정) ⭐ |
| 3 | [캐릭터를 어떤 기술로 움직일 것인가](#3-캐릭터를-어떤-기술로-움직일-것인가) |
| 4 | [캐릭터를 누가 어떻게 그릴 것인가](#4-캐릭터를-누가-어떻게-그릴-것인가) |
| 5 | [알림을 어떻게 띄울 것인가](#5-알림을-어떻게-띄울-것인가) |
| 6 | [강제성을 어디까지 둘 것인가 (제품의 성격)](#6-강제성을-어디까지-둘-것인가-제품의-성격) |
| 7 | [운동을 어떻게 구성할 것인가](#7-운동을-어떻게-구성할-것인가) |
| 8 | [주간 목표를 무엇으로 셀 것인가](#8-주간-목표를-무엇으로-셀-것인가--뒤집힌-결정) ⭐ |
| 9 | [데이터를 어떻게 저장할 것인가](#9-데이터를-어떻게-저장할-것인가) |
| 10 | [앱 상태를 어떻게 표현할 것인가](#10-앱-상태를-어떻게-표현할-것인가) |
| 11 | [문구(다국어)를 어디에 둘 것인가](#11-문구다국어를-어디에-둘-것인가) |
| 12 | [소품(안경 등)을 어떻게 붙일 것인가](#12-소품안경-등을-어떻게-붙일-것인가) |
| 14 | [업데이트를 어떻게 알릴 것인가](#14-업데이트를-어떻게-알릴-것인가) |
| 15 | [육성 게임 요소를 넣을 것인가](#15-육성-게임-요소를-넣을-것인가) |
| 17 | [설정(언어·테마)을 어디에 저장하고 어떻게 반영할 것인가](#17-설정언어테마을-어디에-저장하고-어떻게-반영할-것인가) |
| 18 | [메뉴바 전용 앱에서 설정 창을 어떻게 앞으로 가져올 것인가](#18-메뉴바-전용-앱에서-설정-창을-어떻게-앞으로-가져올-것인가) |
| 19 | [알림을 무시했을 때 어떻게 할 것인가](#19-알림을-무시했을-때-어떻게-할-것인가) |
| 20 | [눈운동을 어떻게 구성할 것인가 (사용해보고 고친 것)](#20-눈운동을-어떻게-구성할-것인가-사용해보고-고친-것) |
| 21 | [햄스터를 놓아둘 자리를 사용자가 정할 수 있게](#21-햄스터를-놓아둘-자리를-사용자가-정할-수-있게) |
| 22 | [산 소품을 어떻게 전달할 것인가](#22-산-소품을-어떻게-전달할-것인가) |
| 24 | [자리에 없는 사람을 어떻게 알아볼 것인가](#24-자리에-없는-사람을-어떻게-알아볼-것인가) |
| 26 | [소스를 공개할 것인가](#26-소스를-공개할-것인가) |
| 27 | [소품 불러오기를 출시 전에 넣을 것인가](#27-소품-불러오기를-출시-전에-넣을-것인가) |

---

## 1. 무엇으로 만들 것인가 (프레임워크)

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **Tauri v2** ✅ | 바이너리 4.2MB, OS 내장 웹엔진 사용, 창·트레이·알림 기본 제공, MIT 라이선스 무료 | Rust 개발환경 설치 필요, 웹뷰 메모리는 여전히 듦 | **채택** |
| Electron | 자료 가장 많음, 크로스플랫폼 완성도 높음 | 앱마다 크롬을 통째로 들고 다녀 150MB+, 상주 앱에 부적합 | 기각 |
| SwiftUI 네이티브 | 가장 가볍고 macOS답다 | mac 전용이라 윈도우 확장 불가, 애니메이션 작업이 웹보다 번거로움 | 보류 |
| 크롬 확장 | 만들기 가장 쉬움, 설치 마찰 없음 | 바탕화면에 못 나감, 크롬 꺼지면 같이 꺼짐, 브라우저 밖 시간을 못 봄 | 기각 |

**결정: AI.** "하루 종일 켜져 있어야 하고 바탕화면에 캐릭터가 떠야 한다"는 조건이
크롬 확장을 원천 배제했고, 남은 둘 중 상주 앱의 무게 조건이 Electron을 배제했다.
윈도우 확장 목표 때문에 SwiftUI도 보류.

---

## 2. 펫 창을 어떤 크기로 띄울 것인가 ⭐ 실측으로 뒤집힌 결정

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **평소 작게(240×240) + 운동할 때만 전체 화면** ✅ | 유휴 CPU 0.43% | 창 크기를 바꾸는 코드가 필요하고 리사이즈 타이밍을 다뤄야 함 | **채택** |
| 항상 전체 화면 투명 창 | 코드가 단순, 햄스터가 어디로든 갈 수 있음 | 유휴 CPU 1.10%. 아무것도 안 움직여도 macOS가 화면 전체를 계속 합성 | 기각 |

**결정: AI (실측 근거).** 처음엔 전체 화면으로 만들었다가 측정하고 뒤집었다.
**이 프로젝트에서 가장 중요한 발견이다: 비용은 애니메이션 방식이 아니라 상시 합성되는 창 면적에서 나온다.**
나중에 기능을 붙이다가 창을 계속 크게 두고 싶어지면 이 숫자를 다시 재야 한다.

---

## 3. 캐릭터를 어떤 기술로 움직일 것인가

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **스프라이트 + 캔버스 직접 그리기** ✅ | 의존성 0개, 런타임 0KB, 픽셀아트에 정확히 맞음, 20년 검증된 방식 | 복잡한 애니메이션은 손이 많이 감 | **채택** |
| Rive | 상태 머신 내장, GPU 렌더, 파일이 Lottie보다 3~5배 작음 | 웹 런타임 200KB(WASM), 에디터가 유료 SaaS, 학습 필요 | 나중 후보 |
| Lottie | 오픈소스, 디자이너 친화적 | SVG 렌더가 CPU를 쓴다. 상주 앱에서 가장 피해야 할 조합, 상호작용 불가 | 기각 |
| 3D 실시간 렌더 (three.js) | 진짜 입체감 | GPU 상시 점유 → "무조건 가벼워야 한다"는 조건과 정면 충돌 | 기각 |

**결정: AI.** 첫 버전에 Rive를 넣을 이유가 없다. 표현이 부족해지면 그때 올린다.
3D가 필요하면 실시간 렌더가 아니라 **Blender에서 만들어 스프라이트로 구워내는** 경로로 간다
(앱은 2D 이미지만 재생하므로 가벼움은 유지된다).

---

## 4. 캐릭터를 누가 어떻게 그릴 것인가

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **코드로 픽셀 도트 작성** ✅ | AI가 직접 만들 수 있음, 즉시 반복 가능, 파일이 250바이트 | 정교한 그림은 한계 | **채택(임시)** |
| Blender 3D → 스프라이트 | 일관된 프레임, 3D 질감 | AI가 조형 품질을 보장할 수 없음. 코드로 좌표를 찍는 방식이라 귀여워지지 않음 | 기각 |
| 애프터이펙트로 2D 제작 | 개발자가 이미 보유·숙련 | 개발자 시간이 듦 | 나중 교체 |
| 에셋 구매/외주 | 품질 확실 | 원하는 동작 조합이 딱 맞는 게 없을 수 있음, 비용 | 나중 후보 |

**결정: AI가 한계를 밝히고 개발자가 수용.** 그래서 **스프라이트는 PNG/맵 교체만으로
바뀌도록** 구조를 짰다. 캐릭터 품질이 개발 진행을 막지 않게 분리하는 게 핵심이다.

---

## 5. 알림을 어떻게 띄울 것인가

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **자체 창 (우하단, 작게)** ✅ | 위치·모양·햄스터 연출을 전부 통제, 방해금지 모드에 안 막힘 | 직접 만들어야 함(다만 펫 창이 이미 있어 추가 비용 적음) | **채택** |
| macOS 네이티브 알림 | 만들기 쉬움, OS 표준 | 버튼을 못 단다(Tauri의 액션 API는 모바일 전용), 위치 지정 불가, 방해금지면 아예 안 뜸 | 기각 |
| 화면 전체 덮기 | 무시 불가능 | 중요한 순간에 한 번 걸리면 그 자리에서 앱이 삭제됨 | 기각 |

**결정: AI(기술 검증) + 개발자(조건 제시).** 개발자가 "집중을 흐트러뜨리면 안 된다"를
조건으로 걸었고, 검증해보니 네이티브 알림은 버튼을 못 달아 미루기 버튼 자체가 불가능했다.

---

## 6. 강제성을 어디까지 둘 것인가 (제품의 성격)

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **미루기를 쉽게 허용하되 횟수를 기록** ✅ | 무시가 습관이 되는 대신 데이터가 남음, 앱이 미움받지 않음 | 강제력이 없어 효과가 사용자 의지에 달림 | **채택** |
| 화면 잠금/강제 중단 | 확실히 쉬게 만듦 | 중요한 순간에 걸리면 즉시 삭제됨 | 기각 |
| 폰을 멀리 두고 걸어가서 끄기 | 눈·다리 문제를 한 동작으로 해결 | 폰을 멀리 둘 수 없는 사용자가 있음 | 기각 |
| 카메라로 거리·자세 감지 | 실제 나쁜 행동의 순간을 포착 | 여러 PC를 쓰면 쓸모가 없어짐, 웹캠 상시 점유는 배터리·거부감 | 기각 |

**결정: 개발자.** 폰 방식과 카메라 방식을 모두 개발자가 자신의 사용 환경을 근거로 기각했다.
남은 설계가 "무시를 막지 않고 기록한다"이고, 이게 이 제품의 성격을 결정한다.

---

## 7. 운동을 어떻게 구성할 것인가

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **한 세트 (눈 20초 + 기지개 10초)** ✅ | 알림 횟수가 늘지 않음, 타이머 하나, 설정 UI 단순 | 사용자가 시간 배분을 못 정함 | **채택** |
| 눈·스트레칭 각각 다른 주기 | 의학적으로 더 맞음(눈은 자주, 몸은 가끔) | 알림이 두 배, 타이머 2개 + 겹칠 때 처리 필요 | 기각 |
| 한 세트 + 비율 슬라이더 | 중간 절충 | 설정이 늘어난 만큼의 값어치가 불확실 | 기각 |

**결정: 개발자.** 제시된 세 안 중 선택. 방해를 최소화하는 쪽이 최우선이라는 판단.

---

## 8. 주간 목표를 무엇으로 셀 것인가 ⭐ 뒤집힌 결정

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **응답률 = 수행 / (수행+미루기+그만+무시)** ✅ | 근무 시간이 들쭉날쭉해도 분모가 같이 줄어 공정하다 | 표본이 적으면 요동친다(2/2=100%) → 알림 5회 미만이면 판단 보류로 표시 | **채택(2026-09-08 변경)** |
| ~~주간 수행 횟수(주 25회)~~ | 계산이 단순 | 근무 시간을 상수로 가정한다. 이틀 일한 주는 아무리 성실해도 못 채우고, 주말까지 앉아 있으면 대충 해도 채워진다 → 목표가 행동이 아니라 '앉아 있은 시간'을 잰다 | 기각(원래 채택했다가 뒤집음) |
| 연속 달성 일수(스트릭) | 동기부여가 셈 | 하루 놓치면 기록이 끊겨 이탈 | 기각 |
| 화면 앞에 있던 시간 대비 | 가장 정확 | 입력 감시가 필요하고 권한·거부감 문제가 있다. 앱이 켜진 것과 사용자가 보고 있는 것은 다르다 | 나중 후보 |

**결정: 개발자(문제 제기) + AI(설계)**

처음에는 "주간 수행 횟수"로 정했다. 뒤집은 이유는 개발자가 지적한 한 문장이었다.
**"근무 시간이 일정하지 않고, 화면 앞에 있는 시간이 길다."**
절대 횟수는 그 사람이 얼마나 오래 앉아 있었는지를 재게 되고, 오래 앉을수록 점수가 오르는 건
이 앱이 하려는 것과 정반대다.

처음 비율을 기각했던 근거("알림을 적게 받은 주가 유리해진다")도 틀렸다.
알림을 적게 받았다는 건 화면을 적게 봤다는 뜻이고, 그런 주에 점수가 높은 건
왜곡이 아니라 정답이다. 이 지표가 묻는 건 **"눈이 쉬어야 했던 순간에 쉬었나"** 하나다.

**분모에 '오늘 그만'과 '무시'를 반드시 포함한다.** 빼면 세 번 미루고 그만 누르는 게
가장 높은 점수를 받는 방법이 된다. 이 규칙은 `tools/test-events.js`가 지킨다.

## 9. 데이터를 어떻게 저장할 것인가

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **사건만 JSON에 append, 통계는 읽을 때 계산** ✅ | 원본이 남아 나중에 기준이 바뀌어도 과거 데이터를 다시 쓸 수 있음 | 기록이 아주 많아지면 계산이 느려짐(수년치라도 문제없는 규모) | **채택** |
| 집계값을 미리 계산해 저장 | 읽기가 빠름 | 반드시 원본과 어긋난다, 목표 기준을 바꾸면 과거 데이터를 못 씀 | 기각 |
| SQLite 등 DB | 질의가 강력 | 이 규모에 과함, 의존성·마이그레이션 부담 | 기각 |

**결정: AI.** 규모가 작을 때 원본 로그만 남기는 게 거의 항상 옳다.

---

## 10. 앱 상태를 어떻게 표현할 것인가

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **상태 하나 (상태 머신)** ✅ | "미루는 중인데 운동 중" 같은 불가능한 조합이 애초에 표현되지 않음 | 상태 전환을 명시적으로 다 적어야 함 | **채택** |
| 불리언 여러 개 (`isSnoozed`, `isExercising`…) | 처음엔 짜기 쉬움 | 조합이 2ⁿ으로 늘고 모순 상태가 생김. 처음 짤 때 가장 흔히 저지르는 실수 | 기각 |

**결정: AI.** 상태가 5개뿐이라 상태 머신 비용이 거의 0이다.

---

## 11. 문구(다국어)를 어디에 둘 것인가

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **`src/i18n.js` 한 곳. 메뉴바 문구도 JS가 Rust로 밀어넣음** ✅ | 새 언어를 넣을 때 고칠 파일이 하나 | 앱 시작 직후 아주 짧게 기본(영어) 메뉴가 보임 | **채택** |
| Rust와 JS가 각자 문구를 가짐 | 각자 독립적 | 반드시 한쪽만 고쳐져 갈라진다 | 기각 |
| i18n 라이브러리 도입 | 복수형·날짜 형식 등 지원 | 문구 수십 개 규모에 과함 | 나중 후보 |

**결정: 개발자(한/영 지원 요구) + AI(구조)**

---

## 12. 소품(안경 등)을 어떻게 붙일 것인가

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **오버레이 레이어** ✅ | 몸통 맵을 건드리지 않고 소품만 추가, 조합이 자유로움(안경+모자) | 소품이 몸 동작을 따라가야 하면 좌표를 맞춰줘야 함 | **채택** |
| 소품별로 캐릭터 맵 전체 복제 | 그리기 쉬움 | 동작 6개 × 소품 10개 = 60장, 몸을 고치면 60장을 다 고쳐야 함 | 기각 |

**결정: AI.** 팔 들기(기지개)도 같은 오버레이 방식으로 처리해서 구조를 하나로 통일했다.
소품 판매 계획이 있으니 이 구조가 특히 중요하다.

---

## 14. 업데이트를 어떻게 알릴 것인가

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **앱이 버전만 확인 → 배너 → 브라우저로 다운로드** ✅ | 서명 없이도 됨, 서버 불필요, 코드 20줄 | 사용자가 직접 받아 덮어씌워야 함 | **채택** |
| Homebrew cask | `brew upgrade` 한 번으로 끝, 등록 무료 | 일반 사용자에겐 어려움 | **함께 채택** |
| Tauri 자동 업데이터 | 완전 자동 | 서명이 있어야 안전하게 동작 | 자리만 마련 |
| 이메일 수집 후 공지 | 사용자와 직접 연결됨 | 개인정보 처리 책임 발생, 얻는 게 알림 하나뿐 | 기각 |

**결정: AI 권고 + 개발자 수용.** 특히 이메일 수집은 지금 규모가 감당할 부담이 아니다.

---

## 15. 육성 게임 요소를 넣을 것인가

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **넣지 않음. 사용자가 햄스터에게 할 수 있는 일은 운동뿐** ✅ | 무엇을 하는 앱인지 분명해지고 만들 것이 적다 | 재미 요소가 적음 | **채택** |
| 먹이·꾸미기·재화 등 육성 루프 | 체류 시간 증가 | 앱이 "관리해야 할 일거리"가 되어 그것대로 지쳐서 삭제됨 | 기각 |
| 햄스터가 아프거나 죽는 연출 | 강한 동기 | 죄책감이 생기면 사람은 앱을 고치는 게 아니라 지운다 | 기각 |

**결정: AI 제안 + 개발자 수용.** 최악의 상태는 "지쳐 보임"까지고 언제나 회복 가능해야 한다.
단, **소품(안경 등) 판매**는 육성 루프와 다르다. 관리 부담을 만들지 않고 한 번 사면 끝이라
이 결정과 충돌하지 않는다.

---

## 17. 설정(언어·테마)을 어디에 저장하고 어떻게 반영할 것인가

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **localStorage + 창 간 이벤트 알림** ✅ | 코드가 가장 적음, 펫 창과 설정 창이 같은 출처라 저장소를 그냥 공유 | 이미 떠 있는 다른 창은 변경을 모름 → 이벤트로 알려야 함 | **채택** |
| 설정 파일(JSON) + Rust가 관리 | 앱 데이터와 한곳에 모임, 외부에서 편집 가능 | 읽기·쓰기·감시 코드를 다 만들어야 함. 값 두 개에 과함 | 나중에(설정이 늘면) |
| Rust 쪽에 상태를 두고 JS가 물어보기 | 정본이 하나 | 창이 뜰 때마다 왕복이 필요, 지금 얻는 게 없음 | 기각 |

**결정: 개발자(기능 요구) + AI(구조)**

테마는 **세 가지 상태**로 둔다: 시스템 따름(기본) / 라이트 고정 / 다크 고정.
색은 `theme.css`의 토큰에만 있고, JS는 `html`의 `data-theme` 속성만 갈아끼운다.
"시스템 따름"일 때는 속성을 아예 지워서 CSS가 다시 판단하게 한다.

**다크모드를 지금 넣은 이유**: 지금은 말풍선과 설정 창에만 쓰이지만, 나중에 넣으려면
색을 쓴 파일을 전부 뒤져야 한다. 토큰으로 시작하면 추가 비용이 사실상 0이다.

---

## 18. 메뉴바 전용 앱에서 설정 창을 어떻게 앞으로 가져올 것인가

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **창을 띄우는 동안만 일반(Regular) 앱으로 전환** ✅ | 창이 확실히 앞으로 나오고 cmd+tab도 동작, 닫으면 다시 Dock에서 사라짐 | 전환 코드 몇 줄 필요 | **채택** |
| 그냥 `show()` + `set_focus()` | 코드가 짧음 | Dock에 없는(Accessory) 앱은 macOS가 활성화해주지 않아 창이 뒤에 숨는다. 실제로 이 증상을 겪고 고쳤다 | 기각 |
| 설정 창을 항상 위로 고정 | 확실히 보임 | 설정 창이 다른 작업 위에 계속 떠 있는 건 이상함 | 기각 |

**결정: AI (증상을 보고 수정)**

---

## 19. 알림을 무시했을 때 어떻게 할 것인가

이 항목은 **버그를 고치다 나온 설계**다. 원래 코드에는 알림 창을 띄우면 `prompt` 상태를
벗어나는 길이 버튼밖에 없었다. 즉 **사용자가 알림을 그냥 무시하면 앱이 그 상태에 영원히 멈춰**
이후 알림이 아예 오지 않았다. 가장 흔한 실사용 시나리오(바빠서 무시)가 앱을 조용히 죽이는 경로였다.

| 결정 | 대안 | 왜 이렇게 했나 |
|---|---|---|
| **5분 뒤 자동으로 닫는다** | 계속 띄워둔다 / 2분 / 10분 | 회의 중이거나 잠깐 자리를 비운 경우까지 살리는 선. 처음 2분으로 잡았다가 "짧다"는 판단으로 늘렸다. 짧으면 '자리에 없었을 뿐'인 사람이 무시로 기록된다. 계속 띄워두면 화면 구석을 영구 점유한다 |
| **`ignored`로 따로 기록한다** | 미루기로 뭉뚱그린다 | 적극적으로 미룬 것과 반응조차 없는 것은 사용자 상태가 전혀 다르다. 무시가 쌓이면 알림 방식 자체가 안 맞는다는 신호이므로 나중에 제품 판단에 쓸 수 있다 |
| **다음은 15분이 아니라 원래 주기 뒤에 묻는다** | 미루기처럼 15분 뒤 | 무시한 사람에게 15분 만에 다시 묻는 건 재촉이다. 그들은 "15분 뒤에 달라"고 한 적이 없다 |
| **연속 3번 무시되면 그날은 멈춘다** | 계속 알린다 | 반응이 없는데 계속 띄워봐야 소용이 없고, 자리를 비운 것이라면 기록만 틀어진다. 메뉴바에서 '지금 운동하기'를 고르면 다시 시작된다 |

**결정: AI(제안) + 개발자(승인).** 횟수는 3회, 시간은 개발자가 2분 → 5분으로 조정.

자동 멈춤은 파일에 기록하지 않는다. 사용자가 직접 고른 '오늘 그만'과 달리 앱이 알아서 멈춘
것이라, 앱을 다시 켜면 자연스럽게 재개되는 게 맞다.

---

## 20. 눈운동을 어떻게 구성할 것인가 (사용해보고 고친 것)

첫 버전은 "화면 가장자리 한 바퀴 20초 + 기지개 10초"였다. 실제로 써보니 두 가지가 드러났다.

| 문제 | 고친 방법 | 왜 |
|---|---|---|
| 한 바퀴는 너무 느려 눈이 거의 안 움직인다 | **같은 20초에 두 바퀴** | 화면이 크든 작든 두 바퀴를 돌므로 큰 화면에서는 자연히 더 빨라진다. 속도를 화면 크기에 맞춰 따로 계산할 필요가 없다 |
| 말풍선이 햄스터를 따라다녀 어지럽다 | **말풍선을 화면 한가운데 고정** | 따라다니면 글을 읽으려고 눈이 한 번 더 일한다. 속도를 올리면 이 문제가 더 커진다 |
| 화면 안에서 눈을 굴려봐야 초점 거리는 그대로다 | **가운데에 '먼 곳 보기' 10초 추가** | 20-20-20의 핵심은 원거리 초점이다. 이게 없으면 모양체근이 풀리지 않아 눈 운동이라 부르기 어렵다 |

한 세트가 30초 → **40초**로 늘었다. 알림 횟수를 늘리지 않는다는 원칙(7번)은 그대로다.

**결정: 개발자(문제 발견) + AI(구현).** 셋 다 실제로 한 번 해보고 나온 지적이다.

---

## 21. 햄스터를 놓아둘 자리를 사용자가 정할 수 있게

우하단 고정이면 알림 센터·독·화상회의 창과 겹친다. 그렇다고 항상 끌 수 있게 하면
평소 클릭 통과(작업을 방해하지 않는다는 핵심 성질)를 포기해야 한다.

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **메뉴바에서 '위치 옮기기'를 켠 동안만 끌 수 있다** ✅ | 평소 클릭 통과를 유지하면서도 자유롭게 옮길 수 있다 | 한 단계를 거쳐야 함 | **채택** |
| 언제나 끌 수 있게 | 직관적 | 클릭 통과를 포기해야 해서 작업을 가린다 | 기각 |
| 설정에서 네 모서리 중 고르기 | 구현이 가장 쉬움 | 겹침을 피하려는 사람에게 모서리 4개는 충분하지 않다 | 기각 |

옮긴 자리는 대기 중 햄스터와 **알림 창 모두**에 적용된다. 햄스터는 왼쪽 위에 있는데
알림만 오른쪽 아래에서 뜨면 같은 앱이라는 느낌이 사라진다.
운동 중에는 화면 전체를 쓰므로 이 값과 무관하다.

**결정: 개발자(요구) + AI(방식)**

---

## 22. 산 소품을 어떻게 전달할 것인가

처음에는 "구매 → 라이선스 코드 발급 → 앱에서 검증" 흐름을 전제로 플랫폼을 골랐다.
그런데 그 전제 자체가 이 규모에 맞는지 다시 봤다.

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **소품을 파일로 팔고 앱에서 불러오기** ✅ | 서버·코드 발급·검증이 전부 필요 없다. 앱이 인터넷을 안 쓰는 성질을 지킬 수 있다. 파일 하나 파는 곳이면 어디서든 팔 수 있어 플랫폼 선택지가 넓어진다 | 파일이 복사돼 돌아다닐 수 있다 | **채택** |
| 라이선스 키 발급·검증 | 복사를 어느 정도 막는다 | 검증 API가 있는 플랫폼으로 제한된다(사실상 해외 서비스). 앱이 네트워크를 쓰게 된다 | 기각 |

**결정: AI 제안.** 소액 소품에 복사 방지는 값이 맞지 않는다.
막느라 드는 비용(서버 유지, 코드 관리, 플랫폼 제약)이 잃는 돈보다 크다.
어차피 완벽한 복사 방지는 불가능하고, 이 앱을 쓰는 사람이 소품 파일을 유출하려고
애쓸 동기도 약하다.

소품은 이미 도트 좌표 목록이라 파일로 내보내고 불러오는 게 자연스럽다.

**파일은 전달 수단이지 보관 장소가 아니다.** 한 번 불러오면 앱이 자기 저장소로 복사하고,
받은 파일은 지워도 된다. 사용자가 파일을 관리하게 만들면 그때부터 짐이 된다.

- **세트 단위로 파일 하나.** 소품 다섯 개가 들어 있어도 파일은 하나다(낱개로 팔지 않기로 했으므로).
- **앱을 업데이트해도 유지된다.** 앱 데이터 폴더는 앱과 별개다.
- **컴퓨터를 바꾸면 다시 불러와야 한다.** 그래서 구매처에서 재다운로드가 되는지가
  플랫폼 선택의 확인 항목이다(판매처를 고를 때 확인할 항목).

---

## 24. 자리에 없는 사람을 어떻게 알아볼 것인가

개발자가 자리를 오래 비운 뒤 돌아오니 알림이 떠 있었다. 기록을 보니 자동 닫기는 정상이었고,
**20분마다 떴다가 5분 뒤 닫히기를 세 번 반복**한 것이었다. 즉 없는 사람에게 계속 말을 걸었다.

문제는 두 가지다. ①돌아왔을 때 낡은 알림이 떠 있다 ②없는 동안의 시간이 전부
'무시함'으로 기록돼 **응답률이 거짓이 된다**. 성실한 사용자가 자리를 비웠다는 이유로 점수를 잃는다.

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **시스템 유휴 시간(마지막 입력 이후 경과)을 본다** ✅ | macOS가 그대로 알려준다. 무엇을 눌렀는지는 모르고 "얼마나 안 눌렀나"만 안다. 권한도 필요 없다 | 화면을 보고만 있는 경우(영상 시청)를 자리 비움으로 볼 수 있다 | **채택** |
| 입력을 직접 감시 | 정확 | 접근성 권한이 필요하고 키로거로 오해받는다. 이 앱의 "아무것도 수집하지 않는다"와 정면 충돌 | 기각 |
| 카메라로 사람 있는지 확인 | 가장 정확 | 이미 기각한 방식(ADR 6번). 배터리·거부감 | 기각 |
| 그냥 둔다 | 코드 0줄 | 실제로 사용자가 겪은 문제다 | 기각 |

**기준은 3분.** 이보다 오래 입력이 없으면 알림을 띄우지 않고 다음 주기에 다시 확인한다.
알림이 뜬 뒤에 자리를 뜬 경우에는 **'무시함'으로 기록하지 않는다.** 무시한 게 아니기 때문이다.

**결정: 개발자(문제 발견) + AI(방식)**

부수 효과로 지표가 정확해진다. 응답률의 분모가 "자리에 있을 때 받은 알림"으로 좁혀지므로,
자리를 오래 비우는 사람이 불리해지지 않는다.

---

## 26. 소스를 공개할 것인가

처음에는 "오픈소스·MIT"로 적어뒀지만 그건 정한 적 없이 굳어진 기본값이었다.
개발자가 **"설치하는 사람은 비개발자인데, 그 사람들 입장에서 뭐가 좋냐"**고 물어 다시 봤다.

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **비공개로 시작** ✅ | 되돌릴 수 있다. 나중에 공개는 언제든 되지만 한번 공개한 코드는 못 거둔다 | 개발자 채널에서 얻는 신뢰 신호를 포기 | **채택** |
| 처음부터 공개 | 개발자 커뮤니티에서 신뢰 신호. "인터넷 안 쓴다"를 증명 | 되돌릴 수 없다 | 나중에 가능 |

**결정: 개발자(문제 제기) + AI(정리)**

**비개발자에게 소스 공개는 아무 신호도 아니다.** 그들이 실제로 보는 것은 순서대로
①경고창이 뜨는가(서명) ②만든 사람이 보이는가(SNS 계정) ③설치 설명이 친절한가 이고,
소스 공개는 이 목록 어디에도 없다. 개발자 채널에서만 값어치가 있다.

**"인터넷을 쓰지 않는다"는 소스 없이도 증명된다.** 활동 모니터나 방화벽으로 누구나
네트워크 요청을 볼 수 있다. 랜딩과 README에 "직접 확인해보세요"를 넣었다.
비개발자에게는 소스 링크보다 이쪽이 이해하기 쉽다.

### 배포 구조

- **코드 저장소는 비공개**
- **배포용 공개 저장소를 하나 더** 만들고 `node tools/build-site.mjs`가 만든
  `dist-site/`를 올린다. 랜딩 페이지와 Releases만 들어가고 앱 소스는 없다
- GitHub Pages와 Releases가 그대로 무료로 동작한다
- 문구에서 "오픈소스·MIT"를 걷어냈다

---

## 27. 소품 불러오기를 출시 전에 넣을 것인가

개발자가 **"$99 서명은 안 산다"**고 정했다. 그러면 앱 업데이트가 비싼 행위가 된다.
무서명 앱은 새 버전을 받을 때마다 사용자가 우클릭 → 열기를 다시 해야 한다.

소품 판매에서 앱을 고쳐야 하는 부분은 **"소품 파일 불러오기" 버튼 하나뿐**이다.
그래서 순서를 바꿨다.

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **출시 버전에 불러오기를 넣는다** ✅ | 나중에 소품을 팔 때 앱을 아예 안 건드려도 된다. 무료 소품 배포도 가능해져 앱 업데이트 없이 콘텐츠를 계속 낼 수 있다 | 출시 전 반나절 작업 | **채택** |
| 팔 때 같이 넣는다 | 지금 할 일이 줄어든다 | 그 시점에 기존 사용자 전원이 업데이트해야 하고, 전원이 우클릭 절차를 다시 거친다 | 기각 |

**결정: 개발자(제약 확정) + AI(파급 분석)**

### 파일 포맷

```json
{ "format": 1, "id": "dev", "name": {"ko": "...", "en": "..."},
  "accessories": { "hood": { "name": {...}, "layer": [[x, y, "색키"], ...] } },
  "bases":       { "keyboard": { "name": {...}, "scale": 0.95, "layer": [...] } } }
```

**`format` 번호를 올릴 때도 앱은 옛 번호를 계속 읽어야 한다.** 한 번 판 파일은
영원히 열려야 하기 때문이다. 앱이 못 읽는 새 포맷이면 "앱이 오래됐다"고 알려준다.

받은 파일은 앱 저장소(`packs/`)로 복사한다. 사용자가 원본을 계속 들고 있을 필요가 없다.

### 비개발자가 할 수 있어야 한다

처음 만든 버전은 "설정 창을 열고 → 스크롤해서 → 파일 선택 창에서 다운로드 폴더를 찾아
→ 불러온 뒤 → 체크박스를 따로 켠다"였다. **마지막 단계가 특히 나쁘다.**
불러와도 아무 변화가 없으면 "샀는데 아무 일도 안 일어나요"가 된다.

세 가지를 넣었다.

| 고친 것 | 왜 |
|---|---|
| **불러오면 바로 입힌다** | 체크박스를 따로 켜게 하면 산 사람이 거기서 멈춘다. 제일 중요한 하나 |
| **파일을 더블클릭하면 설치된다** | `.hamsterpack` 확장자를 앱에 연결했다. 내려받은 파일을 더블클릭하는 게 가장 몸에 밴 행동이다 |
| **창에 끌어다 놓아도 된다** | 파일 선택 창에서 폴더를 헤매지 않아도 된다 |

더블클릭은 실제로 확인했다. 파일을 여니 앱이 받아 설치했고 햄스터가 바로 그 소품을 입었다.

---

